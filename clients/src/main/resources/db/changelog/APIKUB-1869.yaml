databaseChangeLog:
- changeSet:
    id: APIKUB-1869
    author: a.murzin
    changes:
    - sql:
        dbms: postgresql
        endDelimiter: \nGO
        splitStatements: true
        sql: ALTER TABLE history_item ALTER COLUMN id DROP DEFAULT;
    - sql:
          dbms: postgresql
          endDelimiter: \nGO
          splitStatements: true
          sql: select setval('"history_item_id_seq"'::regclass, (select MAX("id")+100 FROM "history_item"));
